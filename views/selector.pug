extends layout

block content
  h1= title
  p Welcome to #{title}. My name is Haritha Kumpati
  - var image_names = ["onebhk.jpg","twobhk.jpg","threebhk.jpg","fourbhk.jpg","fivebhk.jpg"]
  - for(let i=0; i<3; i++){
    - var j=Math.floor(Math.random()*5);
    - var img_j=image_names[j];
    image(height="300" width="300" src=`\\images\\${img_j}` alt=`${img_j}` )
  - }